{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "build:nyx": "lerna run build --scope nyx",
    "dev:prometeo": "lerna run dev --scope prometeo",
    "deploy:prometeo": "lerna run deploy --scope prometeo-web"
  },
  "devDependencies": {
    "lerna": "^6.0.3",
    "rollup-plugin-visualizer": "^5.8.3"
  }
}
